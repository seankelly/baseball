CREATE TABLE fielding_gamelogs (
    player_id TEXT NOT NULL,
    game_id TEXT NOT NULL,
    pos INTEGER,
    o INTEGER,
    po INTEGER,
    a INTEGER,
    e INTEGER,
    dp INTEGER,
    tp INTEGER,
    bip INTEGER,
    bf INTEGER
)
